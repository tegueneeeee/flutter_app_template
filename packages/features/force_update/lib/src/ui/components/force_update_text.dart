import 'package:flutter/material.dart';

class ForceUpdateText extends StatelessWidget {
  const ForceUpdateText({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('forceUpdateText');
  }
}
