import 'package:flutter/material.dart';

class ForceUpdateListText extends StatelessWidget {
  const ForceUpdateListText({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('forceUpdateListText');
  }
}
