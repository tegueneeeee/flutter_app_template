import 'package:flutter/material.dart';

class ForceUpdateDetailText extends StatelessWidget {
  const ForceUpdateDetailText({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('forceUpdateDetailText');
  }
}
